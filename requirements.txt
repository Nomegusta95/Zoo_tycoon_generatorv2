# Streamlit Cloud deployment (streamlit_app.py) - the desktop app
# (main.py) has its own separate dependency set (customtkinter/tkinter/
# pyinstaller) that is NOT needed here and must not be added to this
# file, since tkinter has no web-hostable build.
streamlit>=1.38
openpyxl>=3.1
Pillow>=10.0
